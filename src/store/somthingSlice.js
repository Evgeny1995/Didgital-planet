import { createSlice } from "@reduxjs/toolkit";

const anyOneSlice = createSlice({
  name: "slice",
  initialState: {
    InitialDataArray: [],
  },
  reducers: {
    ///Какой-нибудь редьюсер
  },
});
